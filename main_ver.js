const Version = `2018.7.25`;
const Changelog = `
    <br><big><w>Changelog Version <big>${Version}</big></w></big><br><hr>
    <span style="font-size:12px; line-height:18px;">

    <li><span class="changed fixed">fix</span> Resolved an issue where the <span class="code">make a suggestion</span> and <span class="code">report a bug</span> buttons had malformed URLs.</li>
	<li><span class="changed fixed">fix</span> Resolved an issue where items were still removed from chest or store if the player did not have inventory space when buying or taking items.</li>
	<li><span class="changed fixed">fix</span> Resolved an issue where some items did not display the proper formatted difference (i.e. 2.4k instead of -2.4k).</li>
	<li><span class="changed updated">update</span> Changed the <span class="code">item info</span> panel to display the item subtype in place of the item type when available.</li>
	<li><span class="changed new">new</span> Added currency as possible quest reward for some later quests.</li>
    <li><span class="changed new">new</span> Added 18 new game quests.</li>
	<li><span class="changed new">new</span> Added 10 new weapon and 9 new armor enchants for levels 35+.</li>

    </span>
`;

var today = new Date();
